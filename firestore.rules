rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for the sandbox to allow seamless testing of administrative features
    // (such as manual approval/decline of bank transfers) across guest and authenticated sessions.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
